export enum EActive {
    HOME = "/",
    SEARCH = "/search",
    SMS = "/messages",
    NOTE = "/notes",
    MEDICALCARD = "/medical-card",
    LOCATION = "/location",
    SETTINGS = "/settings",
    LOGOUT = "/logout",
}
